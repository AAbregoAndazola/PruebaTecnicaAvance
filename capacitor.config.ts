import type { CapacitorConfig } from '@capacitor/cli';

const config = {
  appId: 'com.tuempresa.tuapp',
  appName: 'PruebaTecnica',
  webDir: 'dist/prueba-tecnica',
  bundledWebRuntime: false
};

export default config;
